# eslint-plugin-sample-v2

```console
$ npm install --save-dev ...

$ mkdir packages/eslint-plugin-graphql-v2
$ cd packages/eslint-plugin-graphql-v2 && npm init -y
$ npm install -w packages/eslint-plugin-graphql-v2 @graphql-eslint/eslint-plugin
$ npm install -w packages/eslint-plugin-graphql-v2 --save-dev graphql
```
